
import configureStore from './store/configureStore';

/*
// Default data
let container = {
  container_type : undefined,
  container_dim : {
    length : null,
    bredth : null,
    height : null
  }
};

let Case = {
  case_type : undefined,
  case_dim : {
    length : null,
    bredth : null,
    height : null
  }
}; 
*/
const store = configureStore();